============================================

MODULE BASED CONFIGURATION

name these configuration files the same way
you would name the module name. so

MyGeolocationModule.py -> MyGeolocationModule.conf

===========================================

this module will upstream requests onto
a given array of servers.
===========================================

db_type = 'postgres'
db_table = 'products'
db_user = 'postgres'
db_pass = 'qpqpqp18'
db_database = 'my__'
db_host = '127.0.0.1'
ol_broadcast = 'singular'

ol_upstream = 'yes'

# define its servers
# to request from 
# this will be distributed
# in round robin fashion
ol_dispatcher_timeout = 3600
ol_dispatcher_address = '192.168.1.66'
ol_dispatcher_port = '5341'
ol_cluster = '303.12.20.02', '202.102'


db_commit_timeout = 3600

==========================================